YouTube → Facebook Uploader (Black & Gold GUI)

Quick start:
1. Create and activate a Python venv.
   python -m venv venv
   venv\Scripts\activate   # Windows
2. Install dependencies:
   pip install -r requirements.txt
3. Copy .env.example -> .env and replace placeholders with your keys (or fill them in-app Settings).
4. Run:
   python app.py
